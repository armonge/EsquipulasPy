cifTotal = fobTotal + fleteTotal + seguroTotal + otrosgastosTotal

daiTotal = %dai +cifTotal * tipocambio

iscTotal = daiTotal +daiTotal + %isc

ivaTotal = (cifTotal + daiTotal + iscTotal + tsim ) * %iva

isoTotal = cifTotal * 35%

impuestosTotal = daiTotal + iscTotal + ivaTotal

tsimParcial = ( tsimTotal / cifTotal ) * cifParcial

agenciaParcial = ( agenciaTotal / cifTotal ) * cifParcial

almacenParcial = ( almacenTotal / cifTotal ) * cifParcial 

speParcial = speTotal / cifTotal * cifParcial

papeleriaParcial = %papeleria * unidades

